# 前端架构与交互优化方案

> 本文档定义了前端架构设计原则、性能优化策略和最佳实践，确保应用的可扩展性和用户体验。

## 概述

本项目基于 **Next.js 16 + React 19** 构建，采用 App Router 架构，使用 ts-rest 实现类型安全的 API 调用。

**技术栈**：
- **框架**: Next.js 16 (App Router)
- **UI**: React 19 + Tailwind CSS 4
- **状态管理**: React Query (@tanstack/react-query)
- **API 客户端**: ts-rest + React Query
- **表单**: React Hook Form + Zod
- **国际化**: next-intl

---

## 1. 架构设计

### 1.1 目录结构

```
apps/web/
├── app/                    # Next.js App Router 页面
│   ├── (auth)/            # 认证相关页面组（登录、注册）
│   ├── (dashboard)/       # 仪表盘页面组（需要认证）
│   ├── api/               # API 路由（BFF 层）
│   └── layout.tsx         # 根布局
├── components/            # React 组件
│   ├── ui/               # 基础 UI 组件（shadcn/ui）
│   └── features/         # 业务功能组件
├── lib/                   # 工具库
│   ├── api/              # API 客户端（ts-rest）
│   ├── queries/          # React Query hooks
│   ├── actions/          # Server Actions
│   ├── upload/           # 文件上传模块
│   ├── form/             # 表单工具
│   ├── errors/           # 错误处理
│   └── utils/            # 工具函数
├── hooks/                 # 自定义 Hooks
├── providers/            # Context Providers
├── i18n/                 # 国际化配置
└── locales/              # 翻译文件
```

### 1.2 组件分层

| 层级 | 说明 | 位置 | 示例 |
|------|------|------|------|
| UI 组件 | 纯展示组件，无业务逻辑 | `packages/ui/` | Button, Input, Card |
| 功能组件 | 包含业务逻辑的组件 | `components/features/` | UserProfile, FileUploader |
| 页面组件 | 路由对应的页面 | `app/` | Dashboard, Settings |

### 1.3 Server Components vs Client Components

```typescript
// Server Component（默认）- 用于数据获取和静态内容
// app/dashboard/page.tsx
export default async function DashboardPage() {
  const data = await fetchData(); // 服务端数据获取
  return <Dashboard data={data} />;
}

// Client Component - 用于交互和状态
// components/features/counter.tsx
'use client';

import { useState } from 'react';

export function Counter() {
  const [count, setCount] = useState(0);
  return <button onClick={() => setCount(c => c + 1)}>{count}</button>;
}
```

---

## 2. API 调用规范

### 2.1 ts-rest 客户端使用

```typescript
// lib/api/contracts/client.ts
import { initQueryClient } from '@ts-rest/react-query';
import { contract } from '@repo/contracts/api';

export const api = initQueryClient(contract, {
  baseUrl: process.env.NEXT_PUBLIC_SERVER_BASE_URL,
  baseHeaders: {
    'Content-Type': 'application/json',
  },
});
```

### 2.2 React Query Hooks

```typescript
// lib/queries/user.ts
import { api } from '@/lib/api/contracts/client';

// 查询 Hook
export function useUser(userId: string) {
  return api.user.getUser.useQuery({
    queryKey: ['user', userId],
    params: { id: userId },
  });
}

// 变更 Hook
export function useUpdateUser() {
  const queryClient = useQueryClient();

  return api.user.updateUser.useMutation({
    onSuccess: (data, variables) => {
      // 更新缓存
      queryClient.invalidateQueries({ queryKey: ['user', variables.params.id] });
    },
  });
}
```

### 2.3 数据预取（SSR/SSG）

```typescript
// app/user/[id]/page.tsx
import { dehydrate, HydrationBoundary, QueryClient } from '@tanstack/react-query';

export default async function UserPage({ params }: { params: { id: string } }) {
  const queryClient = new QueryClient();

  // 服务端预取数据
  await queryClient.prefetchQuery({
    queryKey: ['user', params.id],
    queryFn: () => fetchUser(params.id),
  });

  return (
    <HydrationBoundary state={dehydrate(queryClient)}>
      <UserProfile userId={params.id} />
    </HydrationBoundary>
  );
}
```

---

## 3. 错误处理

### 3.1 Error Boundary

```typescript
// components/error-boundary.tsx
'use client';

import { Component, ReactNode } from 'react';

interface Props {
  children: ReactNode;
  fallback?: ReactNode;
}

interface State {
  hasError: boolean;
  error?: Error;
}

export class ErrorBoundary extends Component<Props, State> {
  state: State = { hasError: false };

  static getDerivedStateFromError(error: Error): State {
    return { hasError: true, error };
  }

  componentDidCatch(error: Error, errorInfo: React.ErrorInfo) {
    // 上报错误到监控服务
    console.error('ErrorBoundary caught:', error, errorInfo);
  }

  render() {
    if (this.state.hasError) {
      return this.props.fallback || <DefaultErrorFallback error={this.state.error} />;
    }
    return this.props.children;
  }
}
```

### 3.2 API 错误处理

```typescript
// lib/errors/api-error.ts
export class ApiError extends Error {
  constructor(
    public code: number,
    public message: string,
    public data?: unknown,
  ) {
    super(message);
    this.name = 'ApiError';
  }
}

// 在 React Query 中处理
const { data, error } = useUser(userId);

if (error instanceof ApiError) {
  if (error.code === 401) {
    // 重定向到登录
  } else if (error.code === 404) {
    // 显示未找到
  }
}
```

---

## 4. 性能优化策略

### 4.1 代码分割

```typescript
// lib/dynamic-import.tsx
import dynamic from 'next/dynamic';

// 重型组件延迟加载
export const HeavyEditor = dynamic(
  () => import('@/components/features/editor'),
  {
    loading: () => <EditorSkeleton />,
    ssr: false, // 仅客户端渲染
  }
);

// 条件加载
export const AdminPanel = dynamic(
  () => import('@/components/features/admin-panel'),
  { ssr: false }
);
```

### 4.2 图片优化

```typescript
import Image from 'next/image';

// 使用 Next.js Image 组件
<Image
  src="/hero.jpg"
  alt="Hero"
  width={1200}
  height={600}
  priority        // 首屏图片优先加载
  placeholder="blur"
  blurDataURL="data:image/jpeg;base64,..."
/>

// 响应式图片
<Image
  src="/hero.jpg"
  alt="Hero"
  fill
  sizes="(max-width: 768px) 100vw, 50vw"
  className="object-cover"
/>
```

### 4.3 数据缓存策略

```typescript
// React Query 全局配置
// providers/query-provider.tsx
const queryClient = new QueryClient({
  defaultOptions: {
    queries: {
      staleTime: 5 * 60 * 1000,      // 5 分钟内数据视为新鲜
      gcTime: 30 * 60 * 1000,        // 30 分钟后垃圾回收
      retry: 3,                       // 失败重试 3 次
      retryDelay: (attempt) => Math.min(1000 * 2 ** attempt, 30000),
      refetchOnWindowFocus: false,   // 窗口聚焦不自动刷新
    },
  },
});
```

### 4.4 请求去重

```typescript
// React Query 自动去重相同 queryKey 的请求
const { data: user1 } = useUser('123'); // 发起请求
const { data: user2 } = useUser('123'); // 复用上面的请求，不会重复发起
```

---

## 5. 表单处理

### 5.1 React Hook Form + Zod

```typescript
// lib/form/schemas/user.ts
import { z } from 'zod';

export const userFormSchema = z.object({
  name: z.string().min(1, '姓名不能为空'),
  email: z.string().email('邮箱格式不正确'),
  age: z.number().min(0).max(150).optional(),
});

export type UserFormData = z.infer<typeof userFormSchema>;
```

```typescript
// components/features/user-form.tsx
'use client';

import { useForm } from 'react-hook-form';
import { zodResolver } from '@hookform/resolvers/zod';
import { userFormSchema, UserFormData } from '@/lib/form/schemas/user';

export function UserForm() {
  const form = useForm<UserFormData>({
    resolver: zodResolver(userFormSchema),
    defaultValues: { name: '', email: '' },
  });

  const onSubmit = async (data: UserFormData) => {
    await updateUser(data);
  };

  return (
    <form onSubmit={form.handleSubmit(onSubmit)}>
      <input {...form.register('name')} />
      {form.formState.errors.name && (
        <span>{form.formState.errors.name.message}</span>
      )}
      <button type="submit" disabled={form.formState.isSubmitting}>
        提交
      </button>
    </form>
  );
}
```

---

## 6. 文件上传

### 6.1 上传流程

```typescript
// lib/upload/uploader.ts
import { api } from '@/lib/api/contracts/client';

export async function uploadFile(file: File, options?: UploadOptions) {
  // 1. 获取上传凭证
  const { token, key, bucket } = await api.uploader.getPrivateToken({
    body: {
      filename: file.name,
      fsize: file.size,
      signature: generateSignature(file),
    },
  });

  // 2. 上传到云存储
  await uploadToStorage(token, file);

  // 3. 确认上传完成
  await api.uploader.complete({
    body: { fileId: key, signature },
  });

  return { key, url: `${bucket}/${key}` };
}
```

### 6.2 分片上传

```typescript
// lib/upload/multipart-uploader.ts
export async function multipartUpload(file: File) {
  const CHUNK_SIZE = 5 * 1024 * 1024; // 5MB
  const chunks = Math.ceil(file.size / CHUNK_SIZE);

  // 1. 初始化分片上传
  const { uploadId, key } = await api.uploader.initMultipart({
    body: { filename: file.name, fsize: file.size },
  });

  // 2. 上传各分片
  const parts = [];
  for (let i = 0; i < chunks; i++) {
    const chunk = file.slice(i * CHUNK_SIZE, (i + 1) * CHUNK_SIZE);
    const { etag } = await uploadChunk(uploadId, key, i + 1, chunk);
    parts.push({ partNumber: i + 1, etag });
  }

  // 3. 完成分片上传
  await api.uploader.completeMultipart({
    body: { uploadId, key, parts },
  });
}
```

---

## 7. 国际化

### 7.1 配置

```typescript
// i18n/config.ts
export const locales = ['zh', 'en'] as const;
export const defaultLocale = 'zh';

// i18n/request.ts
import { getRequestConfig } from 'next-intl/server';

export default getRequestConfig(async ({ locale }) => ({
  messages: (await import(`../locales/${locale}.json`)).default,
}));
```

### 7.2 使用

```typescript
// 服务端组件
import { getTranslations } from 'next-intl/server';

export default async function Page() {
  const t = await getTranslations('common');
  return <h1>{t('title')}</h1>;
}

// 客户端组件
'use client';
import { useTranslations } from 'next-intl';

export function ClientComponent() {
  const t = useTranslations('common');
  return <button>{t('submit')}</button>;
}
```

---

## 8. 优化清单

### 性能优化
- [x] 使用 `next/dynamic` 进行代码分割 → `lib/dynamic-import.tsx`
- [ ] 使用 `next/image` 优化图片加载
- [x] 配置 React Query 的缓存策略 → `providers/query-provider.tsx`
- [x] 实现请求去重和错误重试 → React Query 内置支持
- [x] 使用 Suspense 处理加载状态 → `components/suspense-utils.tsx`

### 用户体验
- [x] 实现 Error Boundary 处理错误 → `components/error-boundary.tsx`
- [x] 添加加载骨架屏（Skeleton） → `components/skeletons.tsx`
- [x] 实现乐观更新（Optimistic Updates） → `lib/queries/optimistic-update.ts`
- [ ] 添加离线支持（Service Worker）

### 代码质量
- [ ] 使用 TypeScript 严格模式
- [x] 使用 Zod 进行运行时验证 → `@repo/contracts` + `@repo/validators`
- [ ] 编写单元测试和集成测试 → `vitest.config.ts` 已配置
- [x] 配置 ESLint 和 Prettier → `eslint.config.mjs`
- [x] 前端日志库 → `lib/logger.ts`

### 已实现的关键文件

| 功能 | 文件路径 | 说明 |
|------|----------|------|
| 动态导入 | `lib/dynamic-import.tsx` | 代码分割工具函数 |
| Error Boundary | `components/error-boundary.tsx` | 错误边界组件 |
| React Query | `providers/query-provider.tsx` | 差异化缓存策略 |
| 日志库 | `lib/logger.ts` | 生产环境日志控制 |
| API 客户端 | `lib/api/contracts/client.ts` | ts-rest 集成 |
| 文件上传 | `lib/upload/` | 分片上传支持 |
| 骨架屏 | `components/skeletons.tsx` | 常用骨架屏预设 |
| Suspense 工具 | `components/suspense-utils.tsx` | 加载边界组件 |
| 乐观更新 | `lib/queries/optimistic-update.ts` | 乐观更新工具函数 |

---

## 参考资源

- [Next.js 官方文档](https://nextjs.org/docs)
- [React Query 文档](https://tanstack.com/query/latest)
- [ts-rest 文档](https://ts-rest.com/)
- [React Hook Form 文档](https://react-hook-form.com/)
- [Zod 文档](https://zod.dev/)
