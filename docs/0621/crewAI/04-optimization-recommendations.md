# Loops 优化项总表（R1–R16 + D1–D9）

> 本文件是**实施循环的唯一追踪源**。每轮实施完成立即更新对应项 `状态` 与 `实施标注`，并运行 [06-regression-checklist.md](06-regression-checklist.md)。
>
> 状态口径见 [README.md](README.md)：`open / ready / in-progress / done / accepted / blocked / designed`。

## P0 — 正确性 & 规则一致性（批次 1，低风险高价值）

| ID      | 标题                                                                                                                                                                                 | 现状证据                                                                                                                                                    | 处置                                                                                                                                                              | 状态       | 实施标注                                                                                                                                                                                                                                                                                                                                                                                |
| ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **R1**  | **Rule 3 合规**：`LoopsNotificationSender` + `LoopsPrProviderClient` 改用 `@nestjs/axios` `HttpService`，迁入 client 边界                                                            | `loops-notification-sender.service.ts:14`、`loops-pr-provider.client.ts:68` 用全局 `fetch`                                                                  | 注入 `HttpModule` + `HttpService`，`firstValueFrom` 转 Promise；保留 webhook 环境变量与 `RECORDED/SKIPPED/FAILED` 语义；更新两个 spec                             | `done`     | round1：两 client 加 `@Optional() HttpService`，生产路径走 `firstValueFrom(httpService.post(..., {validateStatus:()=>true}))`；`fetch` 仅作 standalone（`new`/ts-node）兜底。`LoopsModule` import `HttpModule`；`LoopsFileStoreService`/`CliLoopsGitAdapter` 经 DI 注入 Nest 托管实例。新增 4 个 HttpService-path spec（notification 2 + PR 2）。check:architecture/sensitive-logs PASS |
| **R2**  | **Winston 注入**：`LoopsService`/`LoopsFileStoreService`/`LoopsRunnerService`/`LoopsPersistenceService` 接入 `WINSTON_MODULE_PROVIDER`；替换静默 `catch(()=>[])` 为结构化 warn/error | `loops.service.ts`/`loops-file-store.service.ts`/`loops-runner.service.ts`/`loops-persistence.service.ts` 全无日志；file-store `readAllIssues:773` 等静默吞 | 各构造注入 logger；关键路径（缺目录、解析失败、进程非零退出、状态写失败）记 `warn`/`error` 含 issueId/shardId；不改变控制流（仍返回 `[]` 兜底，避免破坏既有测试） | `done`     | round1：4 个 service 均加 `@Optional() WINSTON_MODULE_PROVIDER` + 私有 `log()` helper（no-op for standalone）。落点：`LoopsService.getIssue` 吞错→warn 原始错误；`LoopsFileStoreService.readOptionalJson` 区分 ENOENT（正常）vs 损坏（warn）；`LoopsRunnerService.blockedCommand`/`runCommand` 非零退出→warn；`LoopsPersistenceService.doctor` 有 problem→warn 汇总。控制流零变更       |
| **R3**  | **ID 密码学化**：`createIssueId` 用 `crypto.randomUUID` 取代 `Math.random()`                                                                                                         | `loops.service.ts:1600-1603` `Math.random()` 生成 6 字符后缀                                                                                                | 用 `randomUUID().slice(0,8)` 或 `crypto.randomBytes` hex；保持对外 ID 形态（`issue-<8hex>`）兼容；补单测                                                          | `done`     | round1：`randomUUID().replace(/-/g,'').slice(0,8)`（8 hex/日），保持 `issue-<date>-<8hex>` 形态。主链冒烟绿（createIssue 仍产出合法 ID）                                                                                                                                                                                                                                                |
| **R4**  | **去重 commitShard**：`finalize` 遍历提交时跳过已提交 shard；`commitShard` 幂等（无变更→no-op，不报错）                                                                              | `reviewShard:1437-1451`（PASS 提交）+ `finalize:719-728`（再提交）= 重复                                                                                    | finalize 仅对 `!shard.committed` 的 shard 调 `commitShard`；adapter 内 `git status --porcelain` 判空，空则跳过提交并记 debug                                      | `done`     | round1：adapter 级幂等 —— `commitShard` 在 `git add` 后跑 `git diff --cached --quiet`，无暂存变更则返回 `{committed:false,'nothing to commit'}` 不再 `git commit`。reviewShard(PASS) 与 finalize 再调均安全 no-op。默认测试 `LOOPS_GIT_COMMIT_PER_SHARD` 未开→早退 SKIPPED，不受影响                                                                                                    |
| **R5**  | **收敛快照修复**：`runLoopUnlocked` 收敛判据用环内最新 `currentDetail` 而非初始 `detail`                                                                                             | `loops.service.ts:414` 用环外快照                                                                                                                           | 环内每次推进后 `readDetail` 刷新或更新局部 shards 视图；判收敛基于最新视图                                                                                        | `done`     | round1：收敛判据由 `detail.shards`/`detail.state` 改为 `currentDetail.shards`/`currentDetail.state`（环内已每轮 `syncAndRead` 刷新）。主链冒烟绿                                                                                                                                                                                                                                        |
| **R6**  | **Cost guard 覆盖**：补齐 `reviewShard` 的 cost 记账；抽 `costGuardedState` helper 统一 3 个既有成本路径                                                                             | `enforceCostGuard` 仅 `generateSpec`/`decompose`/`persistImplementationRecord`；reviewShard 不计成本                                                        | 抽 `costGuardedState(state, delta)`；reviewShard 经之计 `calls+1`；既有 3 路径改走 helper（行为等价）                                                             | `done`     | round1：新增 `costGuardedState(state,{calls,tokens})` helper；generateSpec/decompose/persistImplementationRecord 改走它（移除内联 costCalls 自增，行为等价）；**reviewShard 新增 `calls+1` 记账**（此前 review-only 路径绕过 cap，现为真实覆盖）。默认 cap 500 calls/5M tokens，测试不触发。token 准确性与 reviewGlobal/reloop 覆盖见 R6b                                               |
| **R6b** | **Cost 剩余覆盖 + token 准确性标记**：`reviewGlobal`(回归)/`reloop` 记账；token 字段标 `estimated`                                                                                   | reviewGlobal/reloop 仍不计成本；CLI token `chars/4` 猜测                                                                                                    | 契约加 `costTokensEstimated` 字段 + 迁移；reviewGlobal/reloop 走 helper                                                                                           | `designed` | round1：依赖契约 schema 变更（`LoopStateItem` 加 estimated 标记）+ 产品确认 review/test 是否计入 cap；默认 cap 极高故低紧迫，留待 schema 迁移窗口                                                                                                                                                                                                                                       |

## P1 — 架构 & 前端（批次 2/3）

| ID      | 标题                                                                                                        | 现状证据                                                                             | 处置                                                                                                                                                                                                | 状态   | 实施标注 |
| ------- | ----------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | -------- |
| **R7**  | **原子状态持久化**：消除 `state.json` 整文件覆写的丢更新竞态                                                | `upsertState:756-765` 整文件覆写，无读改写原子性                                     | 方案 A（首选，低风险）：按 issue 拆 `state/<issueId>.json`，`upsertState` 只写该 issue 文件，`readState` 聚合目录；`doctor` 增加 cross-file 一致性提示。方案 B（更重）：DB 作为主真源。本批取方案 A | `open` | —        |
| **R8**  | **结构化 JSON 解析**：替换 `extractJson` 启发式为健壮解析                                                   | `loops-process.util.ts:106-124` 首尾括号匹配，嵌套尾括号失败                         | 用 `jsonrepair`/ fenced-codeblock 剥离 + `JSON.parse` + Zod `safeParse`；保留回退 deterministic 但记 warn；保留 `extractJson` 作兜底别名                                                            | `open` | —        |
| **R9**  | **分布式 work-lock**：抽象 `LoopsLockBackend`，提供 in-memory（默认）+ Redis backend（`@dofe/infra-redis`） | `loops-work-lock.service.ts` 内存 `Set`，多实例不安全                                | 抽接口 `tryAcquire/release`；默认 in-memory（行为不变，测试绿）；Redis backend 用 `SET NX PX`，`LOOPS_LOCK_BACKEND=redis` 启用；单测 in-memory + 单测 Redis backend（mock 客户端，不发真实连接）    | `open` | —        |
| **R10** | **前端 Zod 校验**：`new-loop-issue-form` 用契约 schema 客户端校验                                           | `new/new-loop-issue-form.tsx` 仅 HTML `required`，失败通用错误                       | 引 `CreateLoopIssueRequestSchema`，提交前 `safeParse`，字段级错误显示；模板 apply 前确认                                                                                                            | `open` | —        |
| **R11** | **前端轮询**：`[issueId]` 详情页 `useLoopIssue` 加 `refetchInterval`（按 phase 动态：活体态短间隔，终态停） | `loops.ts:70-77` 无轮询                                                              | 活体态（`IN_LOOP`/`IN_PROGRESS`）`refetchInterval: 3000`；终态（`CLOSED`/`ARCHIVED`/`REJECTED`）停；可 `enabled` 受 pause 控制                                                                      | `open` | —        |
| **R12** | **前端错误态 / 乐观更新**：仪表盘与详情页区分 loading/error/empty；shard 操作乐观更新 + `onError` 回滚      | `page.tsx:106-109` collapse isLoading/isError；`use-loop-operations.ts` 无 `onError` | 抽 `QueryState` helper；mutation `onMutate` 乐观写缓存 + `onError` 回滚 + invalidate                                                                                                                | `open` | —        |

## P2 — 测试覆盖 & 重构（批次 4/5）

| ID      | 标题                             | 现状证据                                                          | 处置                                                                                                                                                                                         | 状态   | 实施标注 |
| ------- | -------------------------------- | ----------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | -------- |
| **R13** | **补测高风险路径**               | cost guard/reloop 上限/CLI fallback/并发/finalize git/RBAC 未覆盖 | 新增 spec：cost guard trip+resume、reloop 抛错、`autoReloopAfterGlobalReview` 暂停、context-budget block、CLI adapter `LOOPS_AGENT_MODE=cli` fake、work-lock `Promise.all` 竞态、RBAC 权限串 | `open` | —        |
| **R14** | **拆 `LoopsCapabilityRegistry`** | `loops.service.ts:839-1133` 300+ 行静态能力数据                   | 抽为 `loops-capability-registry.ts`（`@Injectable()`），service 注入；纯数据，零行为变更                                                                                                     | `open` | —        |
| **R15** | **拆 `LoopsCoverageService`**    | `loops.service.ts:2036-2159` 需求覆盖文本匹配                     | 抽为独立 service，service 注入；保持匹配语义                                                                                                                                                 | `open` | —        |
| **R16** | **拆 `[issueId]/page.tsx`**      | 1124 行 god 组件                                                  | 拆 `ShardCard`/`SpecPanel`/`RunControls`/`GlobalReviewPanel` 子组件；渐进式，逐个迁移                                                                                                        | `open` | —        |

## Deferred / Blocked（D）—— 重大特性或依赖外部输入

| ID     | 标题                                                            | 依赖/阻塞                                    | 处置                                          | 状态      |
| ------ | --------------------------------------------------------------- | -------------------------------------------- | --------------------------------------------- | --------- |
| D1     | Flow DSL（`@start/@listen/@router` + 可序列化 `loops.flow/v1`） | 产品决策 + 设计                              | designed → 排期                               | `open`    |
| D2     | 状态机形式化（中央转移表 + 守卫）                               | 设计                                         | designed                                      | `open`    |
| D3     | 统一 Memory / Knowledge / RAG                                   | 重大特性 + 设计                              | designed                                      | `open`    |
| D4     | Guardrail 体系（`GuardrailResult` + LLM guardrail + 断路器）    | 设计                                         | designed                                      | `open`    |
| D5     | Checkpoint / Replay（不可变检查点 + branching）                 | 设计                                         | designed                                      | `open`    |
| D6     | 多智能体 / A2A / `createLLM` 工厂                               | 设计 + provider 抽象                         | designed                                      | `open`    |
| D7     | **异步执行 via BullMQ**（controller enqueue + worker + SSE）    | 基础设施决策 + `@dofe/infra-rabbitmq` wiring | designed → 与 R9/R11 协同；**最大生产化缺口** | `open`    |
| D8     | SSE / 流式 UI                                                   | D7 前置                                      | designed                                      | `open`    |
| D9     | OTel / 结构化 span                                              | 可观测平台决策                               | designed                                      | `open`    |
| **B1** | 真实 PR provider 验收 + 真实 diff 自动回收                      | 真实 provider 凭据/环境                      | blocked（沿用 `docs/0620/05-blockers.md`）    | `blocked` |
| **B2** | 真实 CLI token 计量                                             | 真实 provider 计费                           | blocked                                       | `blocked` |
| **B3** | 多实例 worker 池生产验收                                        | 多实例环境 + B1/B2                           | blocked                                       | `blocked` |

## 汇总统计

- 本仓可实施：R1–R16（16 项）
- 设计/排期：D1–D9（9 项）
- 阻塞：B1–B3（3 项，沿用 `docs/0620`）

## 批次 → 项映射（见 [05-implementation-plan.md](05-implementation-plan.md)）

| 批次              | 项                     | 退出条件                                                |
| ----------------- | ---------------------- | ------------------------------------------------------- |
| 批次 1（P0）      | R1, R2, R3, R4, R5, R6 | `pnpm test:api` Loops 全绿；规则违规清零；新增对应 spec |
| 批次 2（P1 架构） | R7, R8, R9             | 持久化并发 spec 绿；解析 spec 绿；锁接口 spec 绿        |
| 批次 3（P1 前端） | R10, R11, R12          | `pnpm test:web` Loops 页面测试绿；build 通过            |
| 批次 4（P2 测试） | R13                    | 高风险路径有 spec 覆盖                                  |
| 批次 5（P2 重构） | R14, R15, R16          | 行为零变更；既有 spec 全绿；god object 行数显著下降     |
