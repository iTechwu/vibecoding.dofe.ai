/**
 * Loops Evidence barrel — `@app/services/loops-evidence`.
 *
 * 结构优化 Step 5：交付证据 / delivery 派生原语（当前：inferWorkflowKind）。
 */
export { LoopsEvidenceModule } from './loops-evidence.module';
export { LoopsEvidenceService } from './loops-evidence.service';
